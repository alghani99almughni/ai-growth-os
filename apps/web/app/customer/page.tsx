"use client";
import {useState} from "react";
export default function Customer(){const [phone,setPhone]=useState("");const [name,setName]=useState("");const [status,setStatus]=useState("");const tenantId=process.env.NEXT_PUBLIC_DEMO_TENANT_ID||"";async function identify(){const base=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";const r=await fetch(base+"/api/v1/customers",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({tenant_id:tenantId,phone,name})});setStatus(r.ok?"You are connected to this business.":"Enter a valid configured tenant.");}return <main className="shell"><div className="hero"><p>CUSTOMER PWA</p><h1>Talk to the business</h1><p>Ask a question, request a service, or become a lead.</p></div><div className="grid"><div className="card"><h2>Your details</h2><input placeholder="Name" value={name} onChange={e=>setName(e.target.value)} style={{width:"100%",padding:12,marginBottom:8}}/><input placeholder="Mobile number" value={phone} onChange={e=>setPhone(e.target.value)} style={{width:"100%",padding:12}}/><button onClick={identify} style={{marginTop:12,padding:10}}>Continue</button><p>{status}</p></div><div className="card"><h2>AI assistant</h2><p>AI chat provider will be connected in the next implementation slice.</p></div></div></main>}
