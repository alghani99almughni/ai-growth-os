"use client";
import {FormEvent,useState} from "react";
export default function Onboarding(){
 const [form,setForm]=useState({name:"",slug:"",industry:"dental"}); const [result,setResult]=useState("");
 async function submit(e:FormEvent){e.preventDefault();const base=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";const r=await fetch(base+"/api/v1/tenants",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const data=await r.json();setResult(r.ok?("Business created: "+data.name+" ("+data.id+")"):(data.detail||"Could not create business"));}
 return <main className="shell"><div className="card" style={{maxWidth:620,margin:"40px auto"}}><p>AI GROWTH OS</p><h1>Create your business</h1><p>Start with one business profile and an industry template.</p><form onSubmit={submit}><label>Business name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} style={{display:"block",width:"100%",padding:12,margin:"6px 0 16px"}}/></label><label>Business slug<input required pattern="[a-z0-9-]+" value={form.slug} onChange={e=>setForm({...form,slug:e.target.value.toLowerCase()})} style={{display:"block",width:"100%",padding:12,margin:"6px 0 16px"}}/></label><label>Industry<select value={form.industry} onChange={e=>setForm({...form,industry:e.target.value})} style={{display:"block",width:"100%",padding:12,margin:"6px 0 16px"}}><option value="dental">Dental</option><option value="restaurant">Restaurant</option><option value="hotel">Hotel</option><option value="salon">Salon</option><option value="gym">Gym</option><option value="real-estate">Real Estate</option></select></label><button type="submit" style={{padding:"12px 18px"}}>Create business</button></form>{result&&<p>{result}</p>}</div></main>}
