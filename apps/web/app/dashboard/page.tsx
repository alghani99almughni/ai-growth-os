"use client";
import {useEffect,useState} from "react";
const api=()=>process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";
const featureLabels:any={digital_menu:"Digital Menu",online_ordering:"Online Ordering",order_tracking:"Order Tracking",call_waiter:"Call Waiter",service_requests:"Service Requests",games:"Mini Games",auto_bill:"Automatic Bill",online_payment:"Online Payment",ai_chat:"AI Chat",ai_voice:"AI Voice",loyalty:"Loyalty Program",referrals:"Referrals",feedback:"AI Feedback",google_review:"Google Review",bookings:"Bookings",queue:"Queue"};
export default function Dashboard(){
 const [tenant,setTenant]=useState<any>(null),[tab,setTab]=useState("overview"),[profile,setProfile]=useState<any>({}),[departments,setDepartments]=useState<any[]>([]),[roles,setRoles]=useState<any[]>([]),[staff,setStaff]=useState<any[]>([]),[features,setFeatures]=useState<any>({}),[menu,setMenu]=useState<any>({categories:[],items:[]}),[orders,setOrders]=useState<any[]>([]),[requests,setRequests]=useState<any[]>([]),[rules,setRules]=useState<any[]>([]),[rewards,setRewards]=useState<any[]>([]),[knowledge,setKnowledge]=useState<any[]>([]),[review,setReview]=useState(""),[status,setStatus]=useState(""),[services,setServices]=useState<any[]>([]),[customers,setCustomers]=useState<any[]>([]),[appointments,setAppointments]=useState<any[]>([]),[referrals,setReferrals]=useState<any>({enabled:false,referrer_points:50,referee_points:25,minimum_purchase:0,message:"Invite a friend and earn wellness rewards."}),[wa,setWa]=useState<any>({provider:"openwa",base_url:"",api_key:"",session_id:"",access_token:"",phone_number_id:"",connected_phone:"",display_name:""}),[waStatus,setWaStatus]=useState<any>(null),[integrations,setIntegrations]=useState<any[]>([]),[integrationCatalog,setIntegrationCatalog]=useState<any[]>([]),[selectedIntegration,setSelectedIntegration]=useState<any>(null),[integrationProvider,setIntegrationProvider]=useState(""),[integrationConfig,setIntegrationConfig]=useState<any>({}),[integrationAccount,setIntegrationAccount]=useState<any>({name:"",id:""}),[waPhone,setWaPhone]=useState(""),[waMethod,setWaMethod]=useState("pairing"),[waPairing,setWaPairing]=useState<any>(null);
 const token=()=>localStorage.getItem("ago_access_token")||"";
 const headers=()=>({Authorization:"Bearer "+token()});
 async function load(){const t=tenant;if(!t)return;const h=headers();const [f,m,o,r,l,lr,g,k,w,bws,ints,cat,sv,cu,ap,rf]=await Promise.all([fetch(api()+"/api/v1/tenants/"+t.id+"/features",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/menu",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/orders",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/service-requests",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/loyalty-rules",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/loyalty-rewards",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/review-settings",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/knowledge",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/integrations/whatsapp",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/integrations/whatsapp/builtin/status",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/integrations",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/integrations/catalog",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/services",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/customers",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/appointments",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/referral-settings",{headers:h})]);if(f.ok)setFeatures((await f.json()).features||{});if(m.ok)setMenu(await m.json());if(o.ok)setOrders((await o.json()).items||[]);if(r.ok)setRequests((await r.json()).items||[]);if(l.ok)setRules((await l.json()).items||[]);if(lr?.ok)setRewards((await lr.json()).items||[]);if(k?.ok)setKnowledge((await k.json()).items||[]);if(g.ok)setReview((await g.json()).review_url||"");if(w.ok){const x=await w.json();setWaStatus(x);setWa((v:any)=>({...v,provider:x.provider,connected_phone:x.connected_phone||"",display_name:x.display_name||""}));} if(bws?.ok){const x=await bws.json();if(x.status&&x.status!=="disconnected")setWaStatus((v:any)=>({...v,...x,provider:"openwa"}));} if(ints?.ok)setIntegrations((await ints.json()).items||[]);if(cat?.ok)setIntegrationCatalog((await cat.json()).items||[]);if(sv?.ok)setServices((await sv.json()).items||[]);if(cu?.ok)setCustomers((await cu.json()).items||[]);if(ap?.ok)setAppointments((await ap.json()).items||[]);if(rf?.ok)setReferrals(await rf.json());
 const pr=await fetch(api()+"/api/v1/tenants/"+t.id,{headers:h});if(pr.ok)setProfile(await pr.json());
 const [ds,rs,ss]=await Promise.all([fetch(api()+"/api/v1/tenants/"+t.id+"/departments",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/roles",{headers:h}),fetch(api()+"/api/v1/tenants/"+t.id+"/staff",{headers:h})]);
 if(ds.ok)setDepartments((await ds.json()).items||[]);if(rs.ok)setRoles((await rs.json()).items||[]);if(ss.ok)setStaff((await ss.json()).items||[]);
}
 useEffect(()=>{const raw=localStorage.getItem("ago_tenant");if(!raw){location.href="/login";return}setTenant(JSON.parse(raw))},[]);
 useEffect(()=>{if(!tenant)return;load();const id=window.setInterval(load,5000);return()=>clearInterval(id)},[tenant]);
 async function saveProfile(){const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/profile",{method:"PUT",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify(profile)});setStatus(r.ok?"Business profile saved":"Unable to save profile");if(r.ok){const x=await r.json();setProfile(x);setTenant(x);localStorage.setItem("ago_tenant",JSON.stringify(x));}}
 async function saveReferrals(){const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/referral-settings",{method:"PUT",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify(referrals)});setStatus(r.ok?"Referral program saved":"Unable to save referrals");}\n async function addService(){const name=prompt("Service / wellness program name");if(!name)return;const price=Number(prompt("Price in INR","0")||0);const description=prompt("Description","")||"";const duration_minutes=Number(prompt("Duration in minutes","30")||30);const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/services",{method:"POST",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({name,price,description,duration_minutes,is_active:true})});setStatus(r.ok?"Service added":"Unable to add service");load();}\n async function addDepartment(){const name=prompt("Department name");if(!name)return;await fetch(api()+"/api/v1/tenants/"+tenant.id+"/departments",{method:"POST",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({name})});load();}
 async function addStaff(){const name=prompt("Staff name");if(!name)return;const email=prompt("Staff email");if(!email)return;const password=prompt("Temporary password (8+ chars)");if(!password)return;const role=prompt("Role: "+roles.map(x=>x.name).join(", "),roles.find(x=>x.name==="staff")?.name||"staff")||"staff";const d=prompt("Department ID (optional): "+departments.map(x=>x.id+"="+x.name).join(", "),"")||null;const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/staff",{method:"POST",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({name,email,password,role,department_id:d||undefined})});setStatus(r.ok?"Staff account created":"Unable to create staff");load();}
 async function saveFeatures(){const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/features",{method:"PUT",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({features})});setStatus(r.ok?"Feature settings saved":"Unable to save features");}
 async function saveWhatsApp(){const payload:any={provider:wa.provider,connected_phone:wa.connected_phone||undefined,display_name:wa.display_name||undefined};if(wa.provider==="openwa")Object.assign(payload,{base_url:wa.base_url,api_key:wa.api_key,session_id:wa.session_id});else Object.assign(payload,{access_token:wa.access_token,phone_number_id:wa.phone_number_id,waba_id:integrationConfig.waba_id||undefined});const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/integrations/whatsapp",{method:"PUT",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify(payload)});const x=await r.json();if(r.ok){setWaStatus(x);setWa((v:any)=>({...v,api_key:"",access_token:""}));setStatus("WhatsApp connection verified and saved");}else setStatus(x.detail||"Unable to connect WhatsApp");}
 async function disconnectWhatsApp(){const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/integrations/whatsapp",{method:"DELETE",headers:headers()});if(r.ok){setWaStatus(await r.json());setStatus("WhatsApp disconnected");}}
 async function loadIntegration(key:any){const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/integrations/"+key,{headers:headers()});const x=await r.json();setSelectedIntegration(key);setIntegrationProvider(x.provider||"");setIntegrationConfig({});setIntegrationAccount({name:x.account_name||"",id:x.account_id||""});}
 async function saveIntegration(){if(!selectedIntegration)return;const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/integrations/"+selectedIntegration,{method:"PUT",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({provider:integrationProvider,mode:integrationProvider==="platform"?"platform":(integrationCatalog.find(x=>x.key===selectedIntegration)?.mode==="oauth"?"oauth":"tenant"),config:integrationConfig,account_name:integrationAccount.name||undefined,account_id:integrationAccount.id||undefined})});const x=await r.json();if(r.ok){setIntegrations(v=>[...v.filter(i=>i.key!==selectedIntegration),x]);setIntegrationConfig({});setStatus(selectedIntegration+" connected");}else setStatus(x.detail||"Unable to save integration");}
 async function disconnectIntegration(key:any){const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/integrations/"+key,{method:"DELETE",headers:headers()});if(r.ok){setIntegrations(v=>v.filter(i=>i.key!==key));setStatus(key+" disconnected");}}
 async function addKnowledge(){const title=prompt("Knowledge title");if(!title)return;const content=prompt("Approved answer/content");if(!content)return;const kind=prompt("Type: faq / policy / product / service / general","faq")||"faq";const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/knowledge",{method:"POST",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({title,content,kind})});setStatus(r.ok?"Knowledge added":"Unable to add knowledge");load();}
 async function saveReview(){const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/review-settings",{method:"PUT",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({review_url:review})});setStatus(r.ok?"Google review settings saved":"Unable to save review settings");}
 async function addCategory(){const name=prompt("Category name");if(!name)return;await fetch(api()+"/api/v1/tenants/"+tenant.id+"/menu/categories",{method:"POST",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({name})});load();}
 async function addItem(){const name=prompt("Menu item name");if(!name)return;const price=Number(prompt("Price in INR","0")||0);await fetch(api()+"/api/v1/tenants/"+tenant.id+"/menu/items",{method:"POST",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({name,price})});load();}
 async function editItem(x:any){const name=prompt("Name",x.name);if(!name)return;const price=Number(prompt("Price in INR",String(x.price))||x.price);const description=prompt("Description",x.description||"")||"";await fetch(api()+"/api/v1/tenants/"+tenant.id+"/menu/items/"+x.id,{method:"PATCH",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({name,price,description})});load()}
 async function toggleItem(x:any){await fetch(api()+"/api/v1/tenants/"+tenant.id+"/menu/items/"+x.id,{method:"PATCH",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({is_available:!x.is_available})});load();}
 async function orderStatus(o:any,status:string){const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/orders/"+o.id,{method:"PATCH",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({status})});if(!r.ok)setStatus("Unable to update order");load();}
 async function requestStatus(x:any,status:string){await fetch(api()+"/api/v1/tenants/"+tenant.id+"/service-requests/"+x.id,{method:"PATCH",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({status})});load();}
 async function addReward(){const name=prompt("Reward name");if(!name)return;const cost=Number(prompt("Points cost","100")||0);const description=prompt("Description","")||"";const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/loyalty-rewards",{method:"POST",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({name,points_cost:cost,description,is_active:true})});setStatus(r.ok?"Reward added":"Unable to add reward");load();}
 async function toggleRule(x:any){const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/loyalty-rules/"+x.id,{method:"PATCH",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({is_active:!x.is_active})});setStatus(r.ok?"Loyalty rule updated":"Unable to update rule");load();}
 async function toggleReward(x:any){const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/loyalty-rewards/"+x.id,{method:"PATCH",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({is_active:!x.is_active})});setStatus(r.ok?"Reward updated":"Unable to update reward");load();}
 async function addRule(){const event=prompt("Event type: purchase / game / referral / first_visit");if(!event)return;const name=prompt("Rule name","Reward")||"Reward";const points=Number(prompt("Points","10")||0);const minimum=Number(prompt("Minimum bill (0 for none)","0")||0);await fetch(api()+"/api/v1/tenants/"+tenant.id+"/loyalty-rules",{method:"POST",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({event_type:event,name,points,config:{minimum_bill:minimum}})});load();}
 if(!tenant)return <main className="shell"><div className="hero"><h1>Loading dashboard…</h1></div></main>;
 return <main className="shell"><section className="hero"><p>AI GROWTH OS • BUSINESS CONTROL CENTER</p><h1>{tenant.name}</h1><p>{tenant.industry} • Configure the customer experience without code.</p></section>
 <nav className="grid" style={{gridTemplateColumns:"repeat(auto-fit,minmax(110px,1fr))"}}>{["overview","profile","website","services","customers","appointments","staff","features","menu","orders","requests","loyalty","referrals","knowledge","whatsapp","integrations","social","reviews"].map(x=><button key={x} className="card" onClick={()=>x==="social"?location.href="/social":x==="website"?location.href="/dashboard/website":setTab(x)}>{x==="overview"?"📊":x==="features"?"🎛️":x==="menu"?"📖":x==="orders"?"🍽️":x==="requests"?"🔔":x==="loyalty"?"🎁":x==="whatsapp"?"💬":"⭐"} {x}</button>)}</nav>
 {tab==="services"&&<section className="card"><div style={{display:"flex",justifyContent:"space-between"}}><div><h2>🧘 Services & Wellness Programs</h2><p>Manage what SS Nutritions offers, pricing and appointment duration.</p></div><button onClick={addService}>+ Add service</button></div><div className="grid">{services.map((x:any)=><article className="card" key={x.id}><h3>{x.name}</h3><p>{x.description||"No description"}</p><strong>₹{x.price??0}</strong><p>{x.duration_minutes||0} minutes • {x.is_active?"Active":"Paused"}</p></article>)}</div></section>}
 {tab==="customers"&&<section className="card"><h2>👥 Customers</h2><p>{customers.length} customers in this tenant CRM.</p><div className="grid">{customers.map((x:any)=><article className="card" key={x.id}><h3>{x.name||"Unnamed"}</h3><p>{x.phone}</p><p>{x.email||"No email"} • {x.source||"unknown source"}</p><small>{x.whatsapp_opt_in?"WhatsApp opted in":"WhatsApp not opted in"}</small></article>)}</div></section>}
 {tab==="appointments"&&<section className="card"><h2>📅 Appointments</h2><p>Upcoming and scheduled customer appointments.</p><div className="grid">{appointments.map((x:any)=><article className="card" key={x.id}><h3>{x.service_name||"Appointment"}</h3><p>{new Date(x.starts_at).toLocaleString()} → {new Date(x.ends_at).toLocaleTimeString()}</p><p>Status: <b>{x.status}</b> • {x.source}</p></article>)}</div></section>}
 {tab==="referrals"&&<section className="card"><h2>🎯 Referral Program</h2><p>The tenant decides whether referrals are active. Configure both sides of the reward without changing code.</p><label><input type="checkbox" checked={!!referrals.enabled} onChange={e=>setReferrals({...referrals,enabled:e.target.checked})}/> Enable referrals</label><div className="grid"><label>Referrer points<input type="number" value={referrals.referrer_points} onChange={e=>setReferrals({...referrals,referrer_points:Number(e.target.value)})}/></label><label>Friend points<input type="number" value={referrals.referee_points} onChange={e=>setReferrals({...referrals,referee_points:Number(e.target.value)})}/></label><label>Minimum purchase<input type="number" value={referrals.minimum_purchase} onChange={e=>setReferrals({...referrals,minimum_purchase:Number(e.target.value)})}/></label><label>Customer message<textarea value={referrals.message} onChange={e=>setReferrals({...referrals,message:e.target.value})}/></label></div><button onClick={saveReferrals}>Save referral program</button></section>}
 {tab==="profile"&&<section className="card"><h2>Business Profile</h2><p>Update your business once; customer-facing experiences read this tenant data.</p><div className="grid">{["name","industry","phone","whatsapp_number","email","address","website","description","timezone"].map(k=><label key={k}>{k.replaceAll("_"," ")}<input value={profile[k]||""} onChange={e=>setProfile({...profile,[k]:e.target.value})}/></label>)}</div><button onClick={saveProfile}>Save Business Profile</button></section>}
 {tab==="staff"&&<section className="card"><div style={{display:"flex",justifyContent:"space-between"}}><div><h2>Staff, Departments & Roles</h2><p>Assign every staff member to a department and role.</p></div><div><button onClick={addDepartment}>+ Department</button><button onClick={addStaff}>+ Staff</button></div></div><div className="grid"><article className="card"><h3>Departments</h3>{departments.map(d=><p key={d.id}><b>{d.name}</b></p>)}</article><article className="card"><h3>Roles</h3>{roles.map(r=><p key={r.id}><b>{r.name}</b><br/><small>{r.permissions.join(", ")}</small></p>)}</article></div><h3>Staff</h3>{staff.map(s=><article className="card" key={s.id}><b>{s.name}</b><p>{s.email} • {s.role} • {s.department_name||"No department"} • {s.is_active?"Active":"Inactive"}</p></article>)}</section>}
 {tab==="overview"&&<section className="grid"><article className="card"><h2>Customer experience</h2><p>{Object.values(features).filter(Boolean).length} enabled capabilities</p><button onClick={()=>setTab("features")}>Configure</button></article><article className="card"><h2>🍽️ Orders</h2><p>{orders.length} recent orders</p><button onClick={()=>setTab("orders")}>Open kitchen board</button></article><article className="card"><h2>🔔 Service</h2><p>{requests.filter(x=>x.status!=="completed").length} open requests</p><button onClick={()=>setTab("requests")}>Open service queue</button></article><article className="card"><h2>🎁 Loyalty</h2><p>{rules.length} earning rules</p><button onClick={()=>setTab("loyalty")}>Manage rewards</button></article></section>}
 {tab==="features"&&<section className="card"><h2>🎛️ Customer Experience</h2><p>Every tenant can turn modules on or off. AI follows these settings.</p><div className="grid">{Object.entries(featureLabels as Record<string,string>).map(([k,label])=><label className="card" key={k}><input type="checkbox" checked={features[k]!==false} onChange={e=>setFeatures({...features,[k]:e.target.checked})}/> {label}</label>)}</div><button onClick={saveFeatures}>Save feature settings</button></section>}
 {tab==="menu"&&<section className="card"><div style={{display:"flex",justifyContent:"space-between"}}><h2>📖 Menu Builder</h2><div><button onClick={addCategory}>+ Category</button><button onClick={addItem}>+ Item</button></div></div><div className="grid">{menu.items.map((x:any)=><article className="card" key={x.id}><h3>{x.name}</h3><p>{x.description||"No description"}</p><strong>₹{x.price}</strong><p>{x.is_available?"🟢 Available":"🔴 Sold out"}</p><button onClick={()=>editItem(x)}>Edit</button><button onClick={()=>toggleItem(x)}>{x.is_available?"Mark unavailable":"Mark available"}</button></article>)}</div></section>}
 {tab==="orders"&&<section className="card"><h2>🍽️ Live Order Board</h2>{orders.map((o:any)=><article className="card" key={o.id}><strong>#{o.id.slice(0,8)} • {o.context_token||"No table"}</strong><p>{o.items.map((i:any)=>i.quantity+"× "+i.name).join(", ")}</p><p>Total ₹{o.total} • <b>{o.status}</b></p><div style={{display:"flex",gap:6,flexWrap:"wrap"}}>{["confirmed","preparing","ready","assigned","served","completed","cancelled"].map(s=><button key={s} onClick={()=>orderStatus(o,s)}>{s}</button>)}</div></article>)}</section>}
 {tab==="requests"&&<section className="card"><h2>🔔 Live Service Requests</h2>{requests.map((x:any)=><article className="card" key={x.id}><strong>{x.request_type}</strong><p>{x.context_token||"No table"} • {x.message||"Customer needs assistance"}</p><p>Status: {x.status}</p>{x.status==="requested"&&<button onClick={()=>requestStatus(x,"acknowledged")}>Acknowledge</button>}{x.status==="acknowledged"&&<button onClick={()=>requestStatus(x,"in_progress")}>In progress</button>}{x.status==="in_progress"&&<button onClick={()=>requestStatus(x,"completed")}>Complete</button>}</article>)}</section>}
 {tab==="loyalty"&&<section className="card"><div style={{display:"flex",justifyContent:"space-between",flexWrap:"wrap",gap:8}}><div><h2>🎁 Tenant Loyalty Program</h2><p>The business owner decides whether loyalty is enabled. Turn it on in <b>Features → Loyalty Program</b>, then configure earning rules and customer rewards here.</p></div><button onClick={()=>setTab("features")}>⚙️ Loyalty setting</button></div>{features.loyalty===false?<article className="card"><h3>🔴 Loyalty is OFF</h3><p>No points should be awarded or redeemed while this tenant setting is disabled.</p></article>:<><div style={{display:"flex",justifyContent:"space-between",flexWrap:"wrap",gap:8}}><h3>Points earning rules</h3><button onClick={addRule}>+ Earning rule</button></div>{rules.map((x:any)=><article className="card" key={x.id}><strong>{x.name}</strong><p>{x.event_type} → +{x.points} points • {x.is_active?"🟢 Active":"⚪ Paused"}</p>{x.config?.minimum_bill>0&&<small>Minimum bill ₹{x.config.minimum_bill}</small>}<div><button onClick={()=>toggleRule(x)}>{x.is_active?"Pause":"Activate"}</button></div></article>)}<div style={{display:"flex",justifyContent:"space-between",flexWrap:"wrap",gap:8}}><h3>Customer rewards</h3><button onClick={addReward}>+ Reward</button></div>{rewards.map((x:any)=><article className="card" key={x.id}><strong>{x.name}</strong><p>Spend {x.points_cost} points • {x.is_active?"🟢 Active":"⚪ Paused"}</p><small>{x.description||"No description"}</small><div><button onClick={()=>toggleReward(x)}>{x.is_active?"Pause":"Activate"}</button></div></article>)}{!rules.length&&!rewards.length&&<p>No loyalty rules or rewards configured yet.</p>}</>}</section>}
 {tab==="knowledge"&&<section className="card"><div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}><div><h2>📚 Business Knowledge Library</h2><p>Put approved business facts here. The AI checks this library before it uses any model tokens.</p></div><button onClick={addKnowledge}>+ Add knowledge</button></div><div className="grid">{knowledge.map((x:any)=><article className="card" key={x.id}><small>{x.kind}</small><h3>{x.title}</h3><p>{x.content}</p></article>)}</div>{knowledge.length===0&&<p>No tenant knowledge yet. Add FAQs, policies, service details, product information and approved answers.</p>}</section>}{tab==="whatsapp"&&<section className="card"><h2>💬 WhatsApp</h2><p>Use our built-in WhatsApp service, or connect your own Meta WhatsApp Cloud API account.</p><label>Provider<select value={wa.provider==="meta"?"meta":"openwa"} onChange={e=>setWa({...wa,provider:e.target.value})}><option value="openwa">Built-in WhatsApp</option><option value="meta">Meta WhatsApp Cloud API</option></select></label>{wa.provider!=="meta"?<><label>Business WhatsApp Number<input value={waPhone} onChange={e=>setWaPhone(e.target.value)} placeholder="+91 9876543210"/></label><label>Connection method<select value={waMethod} onChange={e=>setWaMethod(e.target.value)}><option value="pairing">Phone number pairing code</option><option value="qr">QR code</option></select></label><button onClick={async()=>{const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/integrations/whatsapp/builtin/connect",{method:"POST",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({phone:waPhone,method:waMethod})});const x=await r.json();setWaPairing(x);setStatus(r.ok?(x.pairing_code?"Pairing code generated":"WhatsApp connection started"):(x.detail||"Unable to connect WhatsApp"));}}>{waMethod==="qr"?"Generate QR":"Get Pairing Code"}</button>{waPairing?.pairing_code&&<article className="card"><h3>Pairing Code</h3><strong style={{fontSize:28,letterSpacing:4}}>{waPairing.pairing_code}</strong><p>Open WhatsApp → Linked Devices → Link a Device → Link with phone number instead.</p></article>}{waMethod==="qr"&&<button onClick={async()=>{const r=await fetch(api()+"/api/v1/tenants/"+tenant.id+"/integrations/whatsapp/builtin/qr",{headers:headers()});const x=await r.json();setWaPairing(x);}}>Refresh QR</button>}<p><b>Status:</b> {waStatus?.status||"disconnected"} {waStatus?.connected_phone&&" • "+waStatus.connected_phone}</p></>:<><label>Meta Access Token<input type="password" value={wa.access_token} onChange={e=>setWa({...wa,access_token:e.target.value})} placeholder="Tenant's Meta access token"/></label><label>Phone Number ID<input value={wa.phone_number_id} onChange={e=>setWa({...wa,phone_number_id:e.target.value})} placeholder="Meta phone number ID"/></label><label>WABA ID<input value={integrationConfig.waba_id||""} onChange={e=>setIntegrationConfig({...integrationConfig,waba_id:e.target.value})} placeholder="Optional"/></label><button onClick={saveWhatsApp}>Verify & Connect</button><p><b>Status:</b> {waStatus?.status||"disconnected"} {waStatus?.connected_phone&&" • "+waStatus.connected_phone}</p><p>Connection: {waStatus?.status==="connected"?"● Connected":"Not connected"}</p></>}<p>CRM remains the source of truth. Credentials are encrypted and are never returned by the status API.</p></section>}{tab==="integrations"&&<section className="card"><h2>🔌 Integrations</h2><p>Use the platform services by default, or connect your own provider account. Secrets are encrypted and never shown again.</p><div className="grid">{integrationCatalog.map((x:any)=><article className="card" key={x.key}><h3>{x.name}</h3><small>{x.category}</small><p>{integrations.find(i=>i.key===x.key)?.status==="connected"?"● Connected":"Not connected"}</p><button onClick={()=>loadIntegration(x.key)}>{integrations.find(i=>i.key===x.key)?.status==="connected"?"Manage":"Configure"}</button></article>)}</div>{selectedIntegration&&(()=>{const item=integrationCatalog.find(x=>x.key===selectedIntegration);const oauth=item?.mode==="oauth";return <div className="card" style={{marginTop:16}}><h3>{item?.name}</h3><label>Provider<select value={integrationProvider} onChange={e=>setIntegrationProvider(e.target.value)}>{(item?.providers||[]).map((p:any)=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label>{oauth?<><p>Connect the tenant-owned account securely. We redirect to the provider for consent; your password never enters this platform.</p><button onClick={async()=>{const provider=item?.providers?.[0]?.id;const r=await fetch(api()+"/api/v1/oauth/"+provider+"/start",{method:"POST",headers:{...headers(),"Content-Type":"application/json"},body:JSON.stringify({tenant_id:tenant.id,integration_key:selectedIntegration})});const x=await r.json();if(r.ok&&x.authorization_url)location.href=x.authorization_url;else setStatus(x.detail||"OAuth is not configured");}}>Connect {item?.name}</button><p>Access and refresh tokens are stored encrypted on the server after consent.</p></>:<div className="grid">{selectedIntegration==="razorpay"&&<><label>Key ID<input value={integrationConfig.key_id||""} onChange={e=>setIntegrationConfig({...integrationConfig,key_id:e.target.value})}/></label><label>Key Secret<input type="password" value={integrationConfig.key_secret||""} onChange={e=>setIntegrationConfig({...integrationConfig,key_secret:e.target.value})}/></label><label>Webhook Secret<input type="password" value={integrationConfig.webhook_secret||""} onChange={e=>setIntegrationConfig({...integrationConfig,webhook_secret:e.target.value})}/></label></>}{["gemini","openai","openrouter","anthropic"].includes(selectedIntegration)&&<><label>API Key<input type="password" value={integrationConfig.api_key||""} onChange={e=>setIntegrationConfig({...integrationConfig,api_key:e.target.value})}/></label><label>Model<input value={integrationConfig.model||""} onChange={e=>setIntegrationConfig({...integrationConfig,model:e.target.value})} placeholder="Optional"/></label></>}{selectedIntegration==="email"&&<><label>SMTP Host<input value={integrationConfig.host||""} onChange={e=>setIntegrationConfig({...integrationConfig,host:e.target.value})}/></label><label>SMTP Port<input value={integrationConfig.port||"587"} onChange={e=>setIntegrationConfig({...integrationConfig,port:e.target.value})}/></label><label>Username<input value={integrationConfig.username||""} onChange={e=>setIntegrationConfig({...integrationConfig,username:e.target.value})}/></label><label>Password<input type="password" value={integrationConfig.password||""} onChange={e=>setIntegrationConfig({...integrationConfig,password:e.target.value})}/></label></>}{selectedIntegration==="voice"&&<><label>Provider<input value={integrationConfig.provider||""} onChange={e=>setIntegrationConfig({...integrationConfig,provider:e.target.value})}/></label><label>API Key<input type="password" value={integrationConfig.api_key||""} onChange={e=>setIntegrationConfig({...integrationConfig,api_key:e.target.value})}/></label><label>Phone Number<input value={integrationConfig.phone_number||""} onChange={e=>setIntegrationConfig({...integrationConfig,phone_number:e.target.value})}/></label></>}</div>}<label>Account Name<input value={integrationAccount.name} onChange={e=>setIntegrationAccount({...integrationAccount,name:e.target.value})} placeholder="Business / account name"/></label><div style={{display:"flex",gap:8,flexWrap:"wrap"}}><button onClick={saveIntegration}>Verify & Connect</button><button onClick={()=>disconnectIntegration(selectedIntegration)}>Disconnect</button></div></div>})()}</section>}{tab==="reviews"&&<section className="card"><h2>⭐ Google Review</h2><p>Paste the review link generated from your Google Business Profile.</p><input value={review} onChange={e=>setReview(e.target.value)} placeholder="https://..." /><button onClick={saveReview}>Save</button><p>Only genuine customer feedback should be directed to Google reviews; review incentives are not allowed by Google policy.</p></section>}
 <p aria-live="polite">{status}</p></main>;
}
